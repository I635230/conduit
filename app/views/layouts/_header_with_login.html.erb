<nav class="navbar navbar-light">
  <div class="container">
    <a class="navbar-brand" href="/">conduit</a>
    <ul class="nav navbar-nav pull-xs-right">

      <li class="nav-item">
        <% if yield(:title) != "New Article" && yield(:title) != current_user.username %>
          <a class="nav-link active" href="/">Home</a>
        <% else %>
          <a class="nav-link" href="/">Home</a>
        <% end %>
      </li>

      <li class="nav-item">
        <% if yield(:title) == "New Article" %>
          <%= link_to new_article_path, class: "nav-link active" do %>
            <i class="ion-compose"></i>&nbsp;New Article
          <% end %>
        <% else %>
          <%= link_to new_article_path, class: "nav-link" do %>
            <i class="ion-compose"></i>&nbsp;New Article
          <% end %>
        <% end %>
      </li>

      <li class="nav-item">
        <a class="nav-link" href="#"> <i class="ion-gear-a"></i>&nbsp;Settings </a>
      </li>

      <li class="nav-item">
        <% if yield(:title) == current_user.username %>
          <%= link_to current_user.username, profile_path(current_user.username), class: "nav-link active" %>
        <% else %>
          <%= link_to current_user.username, profile_path(current_user.username), class: "nav-link" %>
        <% end %>
      </li>

      <li class="nav-item">
        <%= link_to "Sign out", logout_path, data: { "turbo-method": :delete }, class: "nav-link" %>
      </li>

    </ul>
  </div>
</nav>